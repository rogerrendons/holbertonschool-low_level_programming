#include "holberton.h"
#include <stdio.h>

/**
 * _sqrt_recursion - check the code for Holberton School students.
 * @n: number to square root.
 * Return: Always 0.
 */

int _sqrt_recursion(int n)
{
	return (mul(n,1));

int mul(n, 1);
if (i * i == n

	if (div <= n)
	{
		printf("res=%d, div=%d, n=%d \n", res, div, n);
		getchar();
		res = _sqrt_recursion(n) / div;
		if ((_sqrt_recursion(n) / div) == res)
		{
			return (div);
		}
		else
		{
			div++;
		}
	}
	return (-1);
}
