#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>

/**
 * main - topic for argc and argv
 *
 *
*/

int main (int argc, char *argv[]__attribute__((unused)))
{

	int valid, res, acum= 0;

	if (argc <= 1)
	{


	}
	else
	{
		printf("Error\n");
		return(1);
	}

	return (0);
}
