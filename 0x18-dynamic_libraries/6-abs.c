#include "holberton.h"
#include "stdlib.h"
/**
* _abs - evalute character c to abs
* @n: The character to print
*
* Return: On success 1.
* On error, -1 is returned, and errno is set appropriately.
*/
int _abs(int n)
{
n = abs(n);
return (n);
}
